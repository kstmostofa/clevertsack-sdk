module.exports = {
    // Your existing ESLint configuration
    env: {
      browser: true, // Ensure browser environment is specified
      node: true,
    },
    globals: {
      SIP: 'readonly', // Declare SIP as a global variable
      lang: 'readonly', // Declare lang as a global variable
      VideoinputDevices: 'readonly', // Declare VideoinputDevices as a global variable (if needed)
    },
    rules: {
      // Your existing rules
    },
  };
  